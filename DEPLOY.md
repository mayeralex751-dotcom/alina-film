# Инструкция по Деплою (Публикации) сайта

Сайт полностью готов и протестирован. Чтобы он стал доступен в интернете (например, `alina-film.netlify.app`), нужно выполнить 4 простых шага.

## Шаг 1. Загрузка кода на GitHub

Я уже подготовил локальный Git-репозиторий и сохранил все изменения. Вам нужно только отправить их в интернет.

1.  **Создайте новый репозиторий** на [GitHub.com](https://github.com/new).
    *   **Repository name**: `alina-film` (или любое другое).
    *   **Public/Private**: Можно выбрать любой (Public бесплатно и проще).
    *   Галочки (README, .gitignore) **НЕ СТАВИТЬ**.
    *   Нажмите **Create repository**.

2.  **Скопируйте ссылку** на репозиторий (она выглядит как `https://github.com/ВАШ_НИК/alina-film.git`).

3.  **Выполните команды в терминале** (скопируйте и вставьте в терминал VS Code):

```bash
# Замените ССЫЛКУ на вашу реальную ссылку с GitHub
git remote add origin https://github.com/ВАШ_НИК/alina-film.git

git branch -M main
git push -u origin main
```

4.  Обновите страницу на GitHub — вы должны увидеть файлы проекта.

---

## Шаг 2. Подключение к Netlify

Netlify — это самый простой сервис для хостинга Astro сайтов.

1.  Зайдите на [app.netlify.com](https://app.netlify.com/) и войдите через **GitHub**.
2.  Нажмите кнопку **"Add new site"** -> **"Import an existing project"**.
3.  Выберите **GitHub**.
4.  Найдите в списке ваш репозиторий `alina-film` и выберите его.

---

## Шаг 3. Настройка и Запуск

Netlify автоматически определит, что это сайт на Astro. Проверьте настройки:

*   **Build command**: `npm run build` (или `astro build`)
*   **Publish directory**: `dist`

Нажмите кнопку **Deploy alina-film**.

Netlify начнет сборку сайта. Это займет около 1 минуты.
Когда статус сменится на **"Published"** (зеленый цвет), сверху появится ссылка вида `random-name-12345.netlify.app`.

Нажмите на нее — **ваш сайт в интернете!** 🎉

---

## Шаг 4. Вход в админку с телефона (Важно!)

Чтобы вы или ваша девушка могли заходить в админку с телефона, нужно включить **Netlify Identity**.

1.  Зайдите в панель управления сайтом на Netlify (там, где "Site settings").
2.  Перейдите во вкладу **"Identity"** (в верхнем меню) и нажмите **"Enable Identity"**.
3.  **Настройка доступа:**
    *   Перейдите в **"Site settings"** -> **"Identity"** -> **"Registration"**.
    *   В разделе **"Registration preferences"** выберите **"Invite only"** (чтобы никто посторонний не смог зарегистрироваться).
4.  **Включение Git Gateway (Чтобы админка могла сохранять фото/тексты):**
    *   Там же, слева в меню выберите **"Services"** -> **"Git Gateway"**.
    *   Нажмите кнопку **"Enable Git Gateway"**.
5.  **Приглашение пользователя:**
    *   Вернитесь на вкладку **"Identity"** (в верхнем меню).
    *   Нажмите **"Invite users"**.
    *   Введите email вашей девушки (и свой email).
    *   На почту придет письмо с ссылкой "Accept the invite".

**Как зайти с телефона:**
1.  Девушка открывает почту на телефоне.
2.  Нажимает на ссылку в письме.
3.  Придумывает пароль.
4.  Сайт открывается. Теперь она может зайти по адресу: `ваш-сайт.netlify.app/admin/`.
5.  Появится кнопка "Login with Netlify Identity" -> вводит email/пароль -> попадает в админку.

---

### Как изменить домен?
В панели Netlify зайдите в **Domain settings** -> **Add custom domain**, если у вас есть купленный домен (например, `alinafilm.com`).
